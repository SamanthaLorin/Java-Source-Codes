/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 */

package com.mycompany.act4;
import java.util.ArrayList;
import java.util.List;
import java.util.Scanner;
/**
 *
 * @author Samantha Lorin
 */

class Vehicle {
    private String brand, model;
    private double distance, fuel;

    public Vehicle(String brand, String model, double distance, double fuel) {
        this.brand = brand;
        this.model = model;
        this.distance = distance;
        this.fuel = fuel;
    }

    public String getBrand() { return brand; }
    public String getModel() { return model; }
    public double getDistance() { return distance; }
    public double getFuel() { return fuel; }

    public double Formula() {
        return fuel == 0 ? 0 : distance / fuel;
    }

    public void VehicleSummary() {
        System.out.println("Vehicle: " + brand + " " + model);
        System.out.println("Distance Traveled: " + distance + " km");
        System.out.printf("Fuel Efficiency: %.2f km/l\n\n",  Formula());
    }
}

class Car extends Vehicle {
    public Car(String brand, String model, double distance, double fuel) {
        super(brand, model, distance, fuel);
    }
}

class Bike extends Vehicle {
    private static final double FUEL_USED = 1.5; 

    public Bike(String brand, String model, double distance) {
        super(brand, model, distance, FUEL_USED);
    }
}

class Truck extends Vehicle {
    public Truck(String brand, String model, double distance, double fuel) {
        super(brand, model, distance, fuel);
    }
}


public class Act4 {

    public static void main(String[] args) {
         Scanner input = new Scanner(System.in);
        List<Vehicle> vehicles = new ArrayList<>();

        // Input for Car
        System.out.println("\nEnter details for a Car:");
        System.out.print("Brand: ");
        String carBrand = input.nextLine();
        System.out.print("Model: ");
        String carModel = input.nextLine();
        System.out.print("Distance Traveled (km): ");
        double carDistance = input.nextDouble();
        System.out.print("Fuel Used (liters): ");
        double carFuel = input.nextDouble();
        input.nextLine(); // Consume newline
        vehicles.add(new Car(carBrand, carModel, carDistance, carFuel));

        // Input for Bike
        System.out.println("\nEnter details for a Bike:");
        System.out.print("Brand: ");
        String bikeBrand = input.nextLine();
        System.out.print("Model: ");
        String bikeModel = input.nextLine();
        System.out.print("Distance Traveled (km): ");
        double bikeDistance = input.nextDouble();
        vehicles.add(new Bike(bikeBrand, bikeModel, bikeDistance));

        // Input for Truck
        System.out.println("\nEnter details for a Truck:");
        System.out.print("Brand: ");
        String truckBrand = input.nextLine();
        System.out.print("Model: ");
        String truckModel = input.nextLine();
        System.out.print("Distance Traveled (km): ");
        double truckDistance = input.nextDouble();
        System.out.print("Fuel Used (liters): ");
        double truckFuel = input.nextDouble();
        vehicles.add(new Truck(truckBrand, truckModel, truckDistance, truckFuel));
        
        System.out.println("\n--- Vehicle Summary ---");
        for (Vehicle vehicle : vehicles) {
            vehicle.VehicleSummary();
        }
    }
}
