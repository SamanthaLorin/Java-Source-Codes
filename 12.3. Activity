// Formula for Mid
mid = (low + high)/2
if(key < mid)
  high = mid - 1
if(key == mid)
  return mid
if(key>mid)  
  low = mid + 1

// Pag Binary Search kailangan naka-arrange from lowest to highest 
// Key can be string 
