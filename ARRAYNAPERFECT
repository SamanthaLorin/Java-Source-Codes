package com.mycompany.somanyarrays;
import java.util.Scanner;
/**
 *
 * @author NUD-Student
 */
public class SoManyArrays {

    public static void main(String[] args) {

        Scanner input = new Scanner(System.in);
        /*
        String[] names = {"Alyssa", "Diane", "Paula", "Jayma"};
        System.out.println(names[0] + ",");
        System.out.println(names[1] + ",");
        System.out.println(names[2] + ",");
        System.out.println(names[3] + " ");
        
        System.out.print("Enter another name: ");
        String addName = input.nextLine();
        
        String[] updatedNames = new String[names.length + 1];
        for (int i = 0; i < names.length; i++) {
            updatedNames[i] = names[i];
        }
        
        updatedNames[names.length] = addName;
        for (String name : updatedNames) {
            System.out.println(name);
        }
        
        System.out.print("Find the Name using Index: ");
        int index = input.nextInt();
        
        System.out.print(names[index]);
        */
    String[][] names = {{"Alyssa", "Diane", "Paula", "Jayma"},
                       {"Nicole 1", "Nicole 2", "Bianca", "Marie"}};
                for (int i = 0; i < names.length; ++i) { 
                for (int j = 0; j < names[i].length; ++j) { 
                System.out.println(names[i][j]);
                }
                
     System.out.print("Enter another name: ");
        String addName = input.nextLine();
        
        String[] updatedNames = new String[names.length + 1];
        for (int a = 0; a < names.length; i++) {
            updatedNames[a] = names[];
        }
        
        updatedNames[names.length] = addName;
        for (String name : updatedNames) {
            System.out.println(name);
        }
        
        System.out.print("Find the Name using Index: ");
        int index = input.nextInt();
        
        System.out.print(names[index]);
                }
    }
}
